apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: promo-test-on-push 
  annotations:
    pipelinesascode.tekton.dev/on-event: "[push]"
    pipelinesascode.tekton.dev/on-target-branch: "[main, promote-dev-stage, promote-stage-prod]" 
    pipelinesascode.tekton.dev/pipeline: ".tekton/do-promotion.yaml"
    pipelinesascode.tekton.dev/task-0: ".tekton/promote.yaml" 
    pipelinesascode.tekton.dev/task-1: "https://raw.githubusercontent.com/redhat-appstudio/tssc-sample-pipelines/main/pac/tasks/git-clone.yaml"
spec:
  params:
  - name: git-url
    value: '{{repo_url}}'   
  - name: revision
    value: '{{revision}}'
  - name: promotion-destination
    value: '{{source_branch}}' 
  - name: event-type
    value: '{{event_type}}'   
  pipelineRef:
    name: do-promotion 
  workspaces:
    - name: git-auth
      secret:
        secretName: "{{ git_auth_secret }}"
    - name: workspace
      volumeClaimTemplate:
        spec:
          accessModes:
          - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi
